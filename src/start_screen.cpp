#include <start_screen.hpp>

void drawText(float x, float y, std::string text)
{
    glRasterPos2f(x, y);
    // glfwBitmapString(GLUT_BITMAP_8_BY_13, (const unsigned char*)text.c_str());
}

void startScreen(GLFWwindow* window){
    // glPrint
}